# Architecture Notes

How a 3rd-Year ECE Student Should Understand the Architecture:
Sensors -> SA-ADC -> DECDE-Fusion -> Decision Tree -> IPM -> LoRa Alert

## Core Principle
Everything else exists only to support these five blocks.
